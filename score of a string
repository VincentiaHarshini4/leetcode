class Solution:
    def scoreOfString(self, s):
        """
        :type s: str
        :rtype: int
        """
        n=0
        for i in range(len(s)-1):
             n+=abs(ord(s[i])-ord(s[i+1]))
        return n
